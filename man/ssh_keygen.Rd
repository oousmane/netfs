% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssh-keygen.R
\name{ssh_keygen}
\alias{ssh_keygen}
\title{Generate an SSH identity file for a connection}
\usage{
ssh_keygen(host, user = NULL, port = 22, path = NULL, overwrite = FALSE)
}
\arguments{
\item{host, user, port}{As passed to \code{\link[=ssh]{ssh()}}; used to build a descriptive
key filename and the suggested \code{ssh-copy-id} command.}

\item{path}{Optional private-key path. Defaults to
\verb{~/.ssh/id_ed25519_<user>_<host>} (or \verb{~/.ssh/id_ed25519_<host>} when
\code{user} is \code{NULL}), with characters unsafe for a filename replaced by
\verb{_}.}

\item{overwrite}{Regenerate an existing key at \code{path}? Default \code{FALSE}
reuses it as-is.}
}
\value{
The identity file path, invisibly. Pass it as
\code{ssh(..., identity_file = )}.
}
\description{
Creates a new ed25519 keypair under \verb{~/.ssh} (reusing one already at the
target \code{path} unless \code{overwrite = TRUE}) and prints the \code{ssh-copy-id}
command that registers the public key on the server. Registering the key
is a one-time step you run yourself; this function never contacts the
server.
}
\examples{
\dontrun{
key <- ssh_keygen("server.example.org", user = "alice")
con <- ssh("server.example.org", user = "alice", identity_file = key)
}
}
\seealso{
Other connection constructors: 
\code{\link{ftp}()},
\code{\link{s3}()},
\code{\link{set_creds}()},
\code{\link{smb}()},
\code{\link{ssh}()},
\code{\link{webdav}()}
}
\concept{connection constructors}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/credentials.R
\name{set_creds}
\alias{set_creds}
\alias{get_creds}
\alias{delete_creds}
\title{Manage credentials for a remote connection}
\usage{
set_creds(con, password = NULL, keyring = NULL)

get_creds(con, keyring = NULL)

delete_creds(con, keyring = NULL)
}
\arguments{
\item{con}{A connection created by \code{\link[=ftp]{ftp()}}, \code{\link[=ssh]{ssh()}}, or \code{\link[=smb]{smb()}}.}

\item{password}{Optional password. When \code{NULL},
\code{\link[keyring:key_get]{keyring::key_set()}} requests it interactively without echoing it.}

\item{keyring}{Optional keyring name. By default, uses \code{NETFS_KEYRING} and
then the system default keyring.}
}
\value{
\code{set_creds()} and \code{delete_creds()} return \code{con} invisibly.
\code{get_creds()} returns a hidden \code{netfs_creds} object.
}
\description{
Credentials are stored in the operating system credential store through
\code{keyring}. The connection object contains no password. Set \code{NETFS_KEYRING}
in \code{.Renviron} to select a named keyring; leave it unset to use the system
default keyring. Never place the password itself in \code{.Renviron}.
}
\examples{
\dontrun{
con <- ftp("ftp.example.org", user = "analyst")
set_creds(con)
get_creds(con)
delete_creds(con)
}
}
\seealso{
Other connection constructors: 
\code{\link{ftp}()},
\code{\link{s3}()},
\code{\link{smb}()},
\code{\link{ssh}()},
\code{\link{ssh_keygen}()},
\code{\link{webdav}()}
}
\concept{connection constructors}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection-s3.R
\name{s3}
\alias{s3}
\title{Create an S3 connection description}
\usage{
s3(
  bucket,
  aws_access_key_id = NULL,
  aws_secret_access_key = NULL,
  region_name = NULL,
  endpoint = NULL,
  profile_name = NULL,
  ...
)
}
\arguments{
\item{bucket}{Bucket name.}

\item{aws_access_key_id}{Optional access key id. Maps to the
connection's stored username - see \code{\link[=set_creds]{set_creds()}}.}

\item{aws_secret_access_key}{Optional secret access key. When supplied,
it is written to the selected \code{keyring} credential store and is not
retained in the connection.}

\item{region_name}{Optional AWS region.}

\item{endpoint}{Optional endpoint URL, for an S3-compatible service
other than AWS (e.g. MinIO).}

\item{profile_name}{Optional named profile to read credentials from,
instead of \code{aws_access_key_id}/\code{aws_secret_access_key}.}

\item{...}{Backend options, passed through to
\code{s3fs::S3FileSystem$new()} (for example \code{s3_force_path_style} for
an S3-compatible service that needs it, or \code{anonymous = TRUE}).}
}
\value{
A \code{netfs_s3} connection.
}
\description{
A connection is scoped to one bucket, the same way \code{\link[=smb]{smb()}} is scoped to
one share - paths are always relative to that bucket's own key
namespace. Support is provided by the s3fs package, used through its
underlying \code{S3FileSystem} object directly rather than its convenience
functions, which share one connection process-wide; a fresh client is
built locally for every call, the same way \code{\link[=ftp]{ftp()}} does. Construction
doesn't contact AWS.
}
\examples{
\dontrun{
s3("my-bucket", region_name = "us-east-1")
}
}
\seealso{
Other connection constructors: 
\code{\link{ftp}()},
\code{\link{set_creds}()},
\code{\link{smb}()},
\code{\link{ssh}()},
\code{\link{ssh_keygen}()},
\code{\link{webdav}()}
}
\concept{connection constructors}

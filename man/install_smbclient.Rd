% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_smbclient}
\alias{install_smbclient}
\title{Install the SMB command-line client}
\usage{
install_smbclient(dry_run = FALSE, ask = TRUE)
}
\arguments{
\item{dry_run}{Display the selected installation commands without executing
them.}

\item{ask}{Ask for confirmation before changing the system. In
non-interactive sessions, explicitly set \code{ask = FALSE} to authorize the
installation.}
}
\value{
\code{TRUE}, invisibly, when \code{smbclient} is available or Windows native
SMB support applies. A dry run returns the installation plan invisibly.
}
\description{
Installs the operating-system package that supplies \code{smbclient} on Linux or
macOS. Windows uses native UNC paths and does not require this executable.
Installation changes the host system and may request administrator access,
so the command is displayed and confirmation is required by default.
}
\examples{
\dontrun{
install_smbclient(dry_run = TRUE)
install_smbclient()
}
}

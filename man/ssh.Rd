% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection-ssh.R
\name{ssh}
\alias{ssh}
\title{Create an SSH connection description}
\usage{
ssh(host, user = NULL, port = 22, password = NULL, identity_file = NULL, ...)
}
\arguments{
\item{host}{Server hostname.}

\item{user}{Optional login name.}

\item{port}{SSH port.}

\item{password}{Optional password to store in \code{keyring}, for callers
that manage authentication themselves; prefer an SSH agent, SSH
configuration, or \code{identity_file} for netfs's own use.}

\item{identity_file}{Optional private-key path.}

\item{...}{Backend options.}
}
\value{
A \code{netfs_ssh} connection.
}
\description{
Authenticate with an SSH agent, SSH configuration, or \code{identity_file}.
OpenSSH runs with \code{BatchMode=yes}, so it never prompts for a password;
\code{password} is stored for callers that authenticate another way, not for
netfs itself. Construction doesn't contact the server.
}
\details{
On Unix, operations on one connection share a single OpenSSH
\code{ControlMaster} session instead of reconnecting on every call. Not
available on Windows, where OpenSSH's \code{ControlMaster} support is
unreliable; each call opens its own connection there instead.
}
\examples{
ssh("server.example.org", user = "user")
}
\seealso{
Other connection constructors: 
\code{\link{ftp}()},
\code{\link{s3}()},
\code{\link{set_creds}()},
\code{\link{smb}()},
\code{\link{ssh_keygen}()},
\code{\link{webdav}()}
}
\concept{connection constructors}
